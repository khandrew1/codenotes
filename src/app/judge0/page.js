"use client";

const Page = () => {
  // const handleRun = () => {
  //     const response = await fetch("/api/execute", { method: "POST", body: JSON.stringify({ source_code: "print('Hello World')", })}
  //     )
  // }

  return <button>Judge0</button>;
};

export default Page;
